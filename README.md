# One-Time-Pad-Encryption
Python script that takes a user inputted plaintext, creates a one time pad key, encrypts the plaintext, displays ciphertext, and decrypts the ciphertext back into the original plaintext.
